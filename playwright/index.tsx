/**
 * Playwright CT Vite entry point.
 * Imports the MDS global stylesheet so Tailwind classes + CSS-variable
 * themes are active in the browser page for all component tests.
 */
import '../app/globals.css';
